{
  "name": "prismy-test-server",
  "version": "0.4.1",
  "description": "E2E test tool for prismy",
  "keywords": [
    "prismy",
    "test",
    "e2e"
  ],
  "author": "Junyoung Choi <rokt33r.choi@gmail.com>",
  "homepage": "https://github.com/BoostIO/prismy-test-server",
  "license": "MIT",
  "main": "dist",
  "files": [
    "dist/**/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BoostIO/prismy-test-server.git"
  },
  "scripts": {
    "build": "rimraf dist && tsc -P tsconfig.build.json",
    "lint": "prettier --check src/**/*.ts specs/*/src/**/*.ts",
    "format": "prettier --write src/**/*.ts specs/*/src/**/*.ts",
    "test": "jest",
    "test-ci": "jest --coverage",
    "codecov": "codecov -f coverage/*.json",
    "prepublishOnly": "npm run lint && npm test && npm run build"
  },
  "bugs": {
    "url": "https://github.com/BoostIO/prismy-test-server/issues"
  },
  "devDependencies": {
    "@types/got": "^9.4.4",
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "@types/test-listen": "^1.1.0",
    "codecov": "^3.5.0",
    "got": "^9.6.0",
    "jest": "^24.8.0",
    "prettier": "^1.17.1",
    "prismy": "^0.12.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.5"
  },
  "peerDependencies": {
    "prismy": ">= 0.12.0 < 1"
  },
  "dependencies": {
    "test-listen": "^1.1.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  }
}
